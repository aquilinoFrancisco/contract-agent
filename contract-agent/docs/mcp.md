# Model Context Protocol (MCP)

Description of the MCP layer: what MCP is, how the `agent_mcp` server is structured, and how each tool (`read_file`, `search_clause`, `save_report`) is registered and called.
